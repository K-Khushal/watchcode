{
  "name": "watchcode",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "bin": {
    "watchcode": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@watchcode/daemon": "workspace:*",
    "@watchcode/hook": "workspace:*",
    "@watchcode/shared": "workspace:*",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
